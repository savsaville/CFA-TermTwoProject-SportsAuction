<%= simple_form_for(@bid) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :bid_amount %>
    <%= f.association :user %>
    <%= f.association :item %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
