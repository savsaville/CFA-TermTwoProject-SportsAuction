<% if @item.has_auction? && @item.auction.top_bid %>
  <% if @item.auction.top_bid.user_id == current_user.id %>
    <p>You won the Auction with a bid of $<%= @item.auction.current_bid %></p>
    <p>
      <%= link_to "Claim Prize", transfer_item_path(@item), class: "btn btn-primary", method: :put %>
    </p>
  <% else %>
    <p>You lost the auction :(</p>
  <% end %>
<% end %>
